A array that is
- O(log n) read/write
- O(log n) slice
- O(log n) merge
- O(1) clone 

something avl tree based??

implement first with trivial data structure to ensure feasibility

Use de bruijn indexing, put everything on stack
